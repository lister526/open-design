# 🔮 Mystica —「全套系统」总索引（从这里开始）

你好。你要的**一整套创业系统**都在这里了：**能永久上线的网站** + **创业必备的全部长篇文件**。
这份索引告诉你**每个文件是什么、按什么顺序用**。

---

## 一、你最关心的：永久网站链接 👉
- 打开 **`DEPLOY_PERMANENT.md`** —— 三条免费路子拿到**永久有效**的网址。
- **最快 3 步**：我已经把可上线的成品推到你 GitHub 仓库 `gh-pages` 分支，
  你只需去 `https://github.com/lister526/open-design/settings/pages` 点开 Pages（选 `gh-pages` / root）→
  1–2 分钟后得到 `https://lister526.github.io/open-design/`（永久）。
- 想要自定义域名/正式生产站：给我 Cloudflare Token，我全自动部署到 `mystica.pages.dev`。
- 完全不想碰技术：把 `dist/` 文件夹拖到 Netlify（拖拽即上线）。

> 网站本身已改成**纯静态、零成本、无需服务器**：报告、七国定价、合盘、起名、择日、
> 推荐裂变、下单流程全部在浏览器本地跑通。真收款/真人 AI 报告/发实物时再上后端（已备好）。

---

## 二、创业必备的长篇文件（按使用顺序）

| 顺序 | 文件 | 一句话作用 |
|---|---|---|
| ① | **`BUSINESS_PLAN.md`** | 完整商业计划：模式、护城河、市场、竞争、里程碑、风险 |
| ② | **`MARKET_VALIDATION.md`**（在上级目录） | 市场/支付意愿/现金流的真实验证与诚实结论 |
| ③ | **`FINANCIAL_MODEL.md`** + **`financial_model.csv`** | 可自己改参数的财务算盘：算清能不能赚、赚多少 |
| ④ | **`OPERATIONS_SOP.md`** | 30 天冷启动作战手册 + 日常运营 SOP + 止损纪律 |
| ⑤ | **`AD_CREATIVES.md`** | TikTok/Meta 广告脚本库（多角度多变体，直接拍） |
| ⑥ | **`LANDING_COPY.md`** | 落地页/首屏营销文案（7 语言钩子对照） |
| ⑦ | **`SUPPLY_CHAIN.md`** | 实物供应链对接清单（Dropship→自建三阶段 + 询价模板） |
| ⑧ | **`BRAND_CONTENT.md`** | 品牌指南 + 30 天内容日历 + SEO + 邮件文案 |
| — | **`../LAUNCH_PLAN.md`** | 早期发布计划（历史文档，与 SOP 配合看） |
| — | **`../DEPLOY.md`** | 真后端（Node）部署零代码指南（Railway/Render/Vercel） |

---

## 三、软件系统本身（已就绪，本地已测通）
- 前端：`index.html` / `app.js` / `engine.js`（命盘引擎）/ `i18n.js`（7 语言）/ `pricing.js`（七国定价）
- 静态化：`static-api.js`（让整站无后端也能跑）/ `dist/`（可直接上线的成品）
- 后端（真收款用）：`server.js` / `payment.js` / `store.js`（订单/用户/推荐持久化）
- 页面：`success.html`（订单/报告）/ `legal.html`（隐私/条款/退款/免责，已齐）
- 配置：`.env.example` / `railway.json` / `render.yaml` / `vercel.json` / `Dockerfile`

---

## 四、现在就做的 4 件事（对齐你的目标：3 个月净赚数百万）
1. **上线**：按 `DEPLOY_PERMANENT.md` 拿永久链接（今天就能做完）。
2. **投放**：用 `AD_CREATIVES.md` 出 5 条素材，按 `OPERATIONS_SOP.md` 第 1 周节奏，
   小预算测**墨西哥/巴西/印度**（低 CAC）。
3. **接实物**：按 `SUPPLY_CHAIN.md` 用 Dropship 零库存上 1 款水晶手链（利润引擎）。
4. **看数据**：每天记"访客/付费单/广告花费"，用 `financial_model.csv` 判断 LTV/CAC，
   **≥3 就加预算放大 = 加利润**。

---

## 五、我必须对你诚实的一句话
软件、文件、可上线的网站——**我全部替你备齐了**。
但"净赚数百万/数千万"最终取决于**投放 ROAS 与实物复购/供应链**，这是运营战，不是软件能替你打的。
我把"能不能做"变成了确定的"能"；`OPERATIONS_SOP.md` + `FINANCIAL_MODEL.md` 就是你打赢这场运营战的地图与算盘。
**先花最小的钱，用真实数据验证单位经济为正；一旦为正，加预算就是加利润——那就是你的第一桶金。**

祝你打赢。🔮
