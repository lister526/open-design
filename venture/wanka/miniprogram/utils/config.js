// Set this to your deployed Worker URL for production.
// For local device testing you cannot use localhost; use a tunnel or the deployed URL.
const API_BASE = 'https://wanka.example.workers.dev';
module.exports = { API_BASE };
